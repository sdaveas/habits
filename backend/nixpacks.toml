[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install pipenv", "pipenv install --system --deploy"]

[phases.build]
cmds = ["alembic upgrade head"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
